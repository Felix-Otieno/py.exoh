let sum = 0; //Here we have initialize the variable of sum and declared it to let keyword and assign it to a valuo of 0 which is an integer.

for(i = 1; i <= 5; i++) {
    sum += i; //add the current value of i
}
console.log("The sum is:", sum)